package br.com.starcode.jerichoselector;



public class JerichoUtil {
	
	/*public static interface Callback {
		
		List<Element> listen(Element e);
		
	}

	public static List<Element> walk(Element e, Callback callback) {
		List<Element> result = new ArrayList<Element>();
		for (Object current : e.findAllElements()) {
			List<Element> partialResult = callback.listen((Element) current);
			if (partialResult != null) {
				result.addAll(partialResult);
			}
		}
//		Stack<Element> s = new Stack<Element>();
//		s.push(e);
//		while(!e.isEmpty()) {
//			Element current = s.pop();
//			List<Element> partialResult = callback.listen(current);
//			if (partialResult != null) {
//				result.addAll(partialResult);
//			}
//			for (Object child : e.getChildElements()) {
//				s.push((Element) child);
//			}
//		}
		return result;
	}*/
	
}
